function C = pc(p)
% C = PC(p); generates comparison P quadratures per channel
% assumes output unchanged from input. 
% eg, identity matrix or uniform thermal+unitary

C = xeros(p.M,1);
end                                              %end y2c function